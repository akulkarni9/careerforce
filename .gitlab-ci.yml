pages:
  image: node:alpine
  stage: deploy
  script:
    - cd frontend
    - npm install
    - npm run build
    - cp -r dist ../public    # Changed 'build' to 'dist'
  artifacts:
    paths:
      - public
  only:
    - main